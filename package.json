{
  "name": "cancel-workflow-on-weekdays",
  "version": "1.0.0",
  "description": "This action cancels the workflow if it was run on one of the specified weekdays.",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js --license licenses.txt && terser ./dist/index.js --compress --comments false --output ./dist/index.js"
  },
  "keywords": ["cancel", "block", "workflow", "workflows", "weekday", "weekdays"],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/freenet-actions/cancel-workflow-on-weekdays/issues"
  },
  "homepage": "https://github.com/freenet-actions/cancel-workflow-on-weekdays",
  "dependencies": {
    "@actions/core": "1.10.1",
    "@actions/github": "6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "0.38.0",
    "terser": "5.21.0"
  }
}
